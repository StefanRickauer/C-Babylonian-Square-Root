# C-Babylonian-Square-Root

In order to install and run the program, type (within the project directory):
  make
  ./bin/bsq <radicant> [--verbose] 
  e.g.
  ./bin/bsq 4 --verbose
  or
  ./bin/bsq 4
  
Delete the binaries with
  make clean
